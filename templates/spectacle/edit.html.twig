{% extends 'layout_admin.html.twig' %}

{% block title %}Modifier un Spectacle{% endblock %}

{% block body %}
    <div class="container-content">
        <h1 class="title">Modifier un spectacle</h1>

        <div class="container mb-5">
            <a href="{{ path('spectacle_index') }}">
                <button class="btn btn-secondary"><i class="fas fa-reply mr-2"></i> Retour aux spectacles</button>
            </a>
        </div>

        <div class="container adminUpdateButton">
            {{ include('spectacle/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>
{% endblock %}
