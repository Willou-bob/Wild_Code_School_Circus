{% extends 'layout.html.twig' %}

{% block title %}<h2 class="m-5">Les acteurs</h2>{% endblock %}

{% block body %}

    <div class="actor">
        <div class="container-fluid">
            <div class="row no-gutters">
                <div class="col-lg-12 col-xs-1">

                    <div class="justify-content-around d-flex flex-wrap">

                        {% for actor in actors %}

                            <div class="card m-5" style="width: 25rem;">
                                <img src="{{ actor.picture }}" class="card-img-top" alt="image-spectacle">
                                <div class="card-body">
                                    <h5 class="card-title">{{ actor.firstname }}</h5>
                                    <p class="card-text">{{ actor.lastname }}</p>
                                </div>
                                <ul class="list-group list-group-flush">
                                    <li class="list-group-item">Rôle: {{ actor.role }}</li>
                                </ul>
                            </div>

                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
